# subdir('python_bindings')

lib{mesh_provider}:
{
  cpp{*}
  $libs_up_to_now
}

{h hpp}{*}: install

libs_upto_now += lib{mesh_provider}

lib{mesh_provider}:
{
  cxx_flags = pkg-config("gismo", "--cflags")
  link_flags = pkg-config("gismo", "--libs")
}

