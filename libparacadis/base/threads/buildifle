# subdir('python_bindings')

./: \
  dedicated_thread_scope \
  locks \
  message_queue \
  python_bindings \
  safe_structs

lib{threads}: \
  cpp{*} \
  $libs_up_to_now

{h hpp}{*}: install

libs_upto_now += lib{threads}

